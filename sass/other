/* @for $i from 1 through list.length($bubble-border-colors){
  .bubble-color-#{$i}{
    background: list.nth($bubble-core-colors, $i);
    border: 1px solid list.nth($bubble-border-colors, $i);
    
    &:active{
      background: list.nth($bubble-border-colors, $i);
    }
  } 
  
}*/


/* $color-name:"light-#{$cur-color}";
    $cur-color:lighten($cur-color, 30%);
    
    
    $lightened-colors:map.set($lightened-colors, $color-name, $cur-color);
    
    $cur-color:transparentize($cur-color, 0.25);
    
    $bubble-core-colors:list.append($bubble-core-colors, $cur-color, $separator:comma);
  */
  
  
  <%- include("./Partials/trapezium.ejs", 
    
      {className:"", color:"fill-#color#-50", reflect:false,
      
      content:"I took initiative in building a training program by communicating with our parent company, in the US, thus establishing a training plan for myself and (then) future employees."}) %>
    
    <%- include("./Partials/trapezium.ejs", 
    
      {className:"", color:"fill-#color#-50", reflect:true,
      
      content:"Developed custom Python scripts for binary data mapping, reducing vehicle support integration time by 50%."}) %>
      
      
      
  <script>
                       
    $(".spinning-circle-container > div").on("click", (e)=>{
    const eduArray = ["1st Class Masters Degree in Automotive Engineering",
                       "",
                       "<ul><li>A - Maths</li><li>B - Physics</li><li>B - Biology</ul>"];
      let t = e.target;
      
      //Make for t is equal to the button image element, when when div is pressed
      
      const unPress = (t)=>{
        $(".spinning-circle-container > div > div").removeClass("active border-3-white")
        $(".spinning-circle-container > div > div").addClass("border-3-#color#-20")
        $(t).parent("div").addClass("active border-3-white");
        $(t).parent("div").removeClass("border-3-#color#-20");
        $(".spinning-circle-container").addClass("show");
        $(".education").addClass("show");
        
        //$(t).siblings("p").css("display", "block");
      }
      
      
      t = (t.tagName=="IMG")?t:($(t).children("img")[0]);
      
      let i = t?.id?.replaceAll("edu-num-", "")??null;
      const tClassList = Array.from($(t).parent("div")[0]?.classList);
      
      $(".spinning-circle-container").removeClass("animate");
      
      if(tClassList.includes("active"))
      {
        $(".spinning-circle-container > div > div").removeClass("active border-3-white");
        $(".spinning-circle-container > div > div").addClass("3-#color#-20");
       //$(t).siblings("p").css("display", "none");
       $(".spinning-circle-container").removeClass("show");
       $(".education").removeClass("show");
        i=null;
      }else{
        unPress();
      }
      
    
   $( $(".spinning-circle-container .show-edu")[i]).removeClass("show-edu");
    //$(p)?.addClass("show-edu")
    console.log(eduArray[i])

      //i?$("p").removeClass("hidden"):$("p").addClass("hidden");
    $(".education p")[0].innerHTML = eduArray?.[i]??""
    });
    

    $("i.bi.bi-arrow-left").on("click", (e)=>unPress());
    console.log($("i.bi.bi-arrow-left"));
  </script>
  
   /*const experience = ["HP Tuners - Vehicle Software Engineer (5 Years)", "I took initiative in building a training program by communicating with our parent company, in the US, thus establishing a training plan for myself and (then) future employees.", "Developed custom Python scripts for binary data mapping, reducing vehicle support integration time by 50%.", "Added an algorithm to our system to bypass the security protocol of an ECM, without the help of AI or any other existing algorithms.", "Also created other tools for binary patching and analysis, such as byte copying (in ida pro) and tools for finding unique id’s of different operating systems from different car brands. These tools reduce time spent on analysis when patching, adding support or maintaining our product, by as much as 35%.",

  "I helped train many engineers that joined after me, which allowed us to almost double our work output, after they had been trained.",

  "I have worked with a lot of customers who have needed support or extra features added, for them to tune their vehicles with our products."]*/